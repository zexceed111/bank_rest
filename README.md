# 🚀 Система Управления Банковскими Картами

REST API приложение для управления банковскими картами, разработанное на Spring Boot с использованием JWT аутентификации.

## 📋 Описание

Система позволяет:
- Создавать и управлять банковскими картами
- Просматривать карты с поиском и пагинацией
- Выполнять переводы между своими картами
- Управлять пользователями (для администраторов)

## 🛠 Технологии

- **Java 17+**
- **Spring Boot 3.2.0**
- **Spring Security** с JWT
- **Spring Data JPA**
- **PostgreSQL**
- **Liquibase** для миграций БД
- **Docker & Docker Compose**
- **Swagger/OpenAPI** для документации
- **JUnit 5** для тестирования

## 📁 Структура проекта

```
bank_rest/
├── src/
│   ├── main/
│   │   ├── java/com/example/bankcards/
│   │   │   ├── config/          # Конфигурация (Security, OpenAPI)
│   │   │   ├── controller/       # REST контроллеры
│   │   │   ├── dto/              # Data Transfer Objects
│   │   │   ├── entity/           # JPA сущности
│   │   │   ├── exception/        # Обработка исключений
│   │   │   ├── repository/       # JPA репозитории
│   │   │   ├── security/         # JWT и Security компоненты
│   │   │   ├── service/          # Бизнес-логика
│   │   │   └── util/             # Утилиты (шифрование, маскирование)
│   │   └── resources/
│   │       ├── application.yml   # Конфигурация приложения
│   │       └── db/migration/      # Liquibase миграции
│   └── test/                      # Тесты
├── docs/
│   └── openapi.yaml               # OpenAPI спецификация
├── docker-compose.yml             # Docker Compose конфигурация
└── pom.xml                        # Maven зависимости

```

## 🚀 Быстрый старт

### Предварительные требования

- Java 17 или выше
- Maven 3.6+
- Docker и Docker Compose
- PostgreSQL (или использовать Docker Compose)

### Шаг 1: Запуск базы данных

Запустите PostgreSQL через Docker Compose:

```bash
docker-compose up -d
```

Это создаст контейнер PostgreSQL 15 с:
- Базой данных: `bankdb`
- Пользователем: `bankuser`
- Паролем: `bankpass`
- Порт: `5432`

#### Проверка работы БД
```bash
# Проверить статус
docker ps

# Проверить логи
docker logs bank_postgres

# Подключиться к БД
docker exec -it bank_postgres psql -U bankuser -d bankdb
```

### Шаг 2: Настройка приложения

Конфигурация уже настроена в `src/main/resources/application.yml`. При необходимости измените параметры подключения к БД.

### Шаг 3: Запуск приложения

Соберите и запустите приложение:

```bash
mvn clean install
mvn spring-boot:run
```

Или используйте IDE для запуска класса `BankRestApp`.

Приложение будет доступно по адресу: `http://localhost:8080`

### Шаг 4: Доступ к документации API

После запуска приложения откройте Swagger UI:

```
http://localhost:8080/swagger-ui.html
```

Или просмотрите OpenAPI спецификацию:

```
http://localhost:8080/api-docs
```

## 🔐 Аутентификация

### Вход в систему

По умолчанию создан администратор:
- **Username:** `admin`
- **Password:** `admin123`

Для входа отправьте POST запрос:

```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

Ответ содержит JWT токен:

```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "type": "Bearer",
  "username": "admin",
  "role": "ADMIN"
}
```

### Использование токена

Добавьте токен в заголовок запросов:

```bash
curl -X GET http://localhost:8080/api/cards \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

## 📚 API Endpoints

### Аутентификация

- `POST /api/auth/login` - Вход в систему

### Карты (для пользователей)

- `POST /api/cards` - Создать карту
- `GET /api/cards` - Получить список своих карт (с пагинацией и поиском)
- `GET /api/cards/{id}` - Получить карту по ID
- `POST /api/cards/{id}/block` - Заблокировать карту
- `POST /api/cards/{id}/activate` - Активировать карту

### Переводы

- `POST /api/transfers` - Перевод между своими картами

### Администратор

- `POST /api/admin/users` - Создать пользователя
- `GET /api/admin/users` - Получить всех пользователей
- `GET /api/admin/users/{id}` - Получить пользователя по ID
- `DELETE /api/admin/users/{id}` - Удалить пользователя
- `POST /api/admin/cards` - Создать карту для пользователя
- `GET /api/admin/cards` - Получить все карты
- `POST /api/admin/cards/{id}/block` - Заблокировать карту
- `POST /api/admin/cards/{id}/activate` - Активировать карту
- `DELETE /api/admin/cards/{id}` - Удалить карту

## 💳 Примеры использования

### Создание карты

```bash
curl -X POST http://localhost:8080/api/cards \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "cardHolderName": "IVAN IVANOV",
    "expiryDate": "2025-12-31",
    "cardType": "DEBIT"
  }'
```

### Перевод между картами

```bash
curl -X POST http://localhost:8080/api/transfers \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "fromCardId": 1,
    "toCardId": 2,
    "amount": 100.50
  }'
```

### Получение списка карт с пагинацией

```bash
curl -X GET "http://localhost:8080/api/cards?page=0&size=10&status=ACTIVE" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

## 🧪 Тестирование

Запустите тесты:

```bash
mvn test
```

Тесты покрывают:
- Сервисы (CardService, TransferService)
- Утилиты (CardMaskUtil)
- Бизнес-логику переводов и управления картами

## 🔒 Безопасность

- **JWT токены** для аутентификации
- **Шифрование** номеров карт, CVV и PIN-кодов
- **Маскирование** номеров карт при отображении
- **Ролевой доступ** (ADMIN/USER)
- **Валидация** входных данных
- **BCrypt** для хеширования паролей

## 📊 База данных

Миграции Liquibase автоматически создают:
- Таблицу `users` с пользователями
- Таблицу `cards` с картами
- Начального администратора

Миграции находятся в `src/main/resources/db/migration/changelog.xml`

## 🐳 Docker и База данных

### Запуск PostgreSQL

```bash
docker-compose up -d
```

### Параметры подключения к БД

- **Host:** localhost
- **Port:** 5432
- **Database:** bankdb
- **Username:** bankuser
- **Password:** bankpass
- **JDBC URL:** `jdbc:postgresql://localhost:5432/bankdb`

### Управление БД

**Остановка:**
```bash
docker-compose down
```

**Очистка данных:**
```bash
docker-compose down -v
```

**Просмотр логов:**
```bash
docker logs -f bank_postgres
```

**Подключение к БД:**
```bash
docker exec -it bank_postgres psql -U bankuser -d bankdb
```

### Автоматические миграции

При первом запуске приложения Liquibase автоматически:
- Создаст таблицы `users` и `cards`
- Создаст необходимые индексы
- Добавит начального администратора (username: `admin`, password: `admin123`)

Подробнее см. `DATABASE_SETUP.md`

## 📝 Конфигурация

Основные настройки в `src/main/resources/application.yml`:

- Порт приложения: `8080`
- JWT секрет и время жизни токена
- Настройки подключения к БД
- Настройки Swagger

## 🏗 Архитектура

Приложение следует принципам:
- **Layered Architecture** (Controller → Service → Repository)
- **DTO Pattern** для передачи данных
- **Exception Handling** с глобальным обработчиком
- **Dependency Injection** через Spring
- **Transaction Management** на уровне сервисов

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Разработано для демонстрации навыков работы с Spring Boot и REST API.

---

**Примечание:** Для продакшена необходимо:
- Использовать безопасное хранилище для секретных ключей (Vault, KMS)
- Настроить HTTPS
- Добавить rate limiting
- Улучшить шифрование (AES-256-GCM)
- Добавить логирование транзакций
- Настроить мониторинг и алертинг
